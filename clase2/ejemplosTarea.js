// Escriba un programa que sume dos numeros y los muestre en pantalla.

const numero1 = 25;
const numero2 = 75;

const resultadoSuma = numero1 + numero2;

console.log(resultadoSuma); // = MOSTRAR EN PANTALLA

// Escriba una funcion que sume dos numeros y devuelva la suma;
function suma(numero1, numero2) {
  const resultadoSuma = numero1 + numero2;

  return resultadoSuma;
}

function resta(numero1, numero2) {
  const resultadoResta = numero1 - numero2;
  return resultadoResta;
}

// const resultado = suma(25, 75);
